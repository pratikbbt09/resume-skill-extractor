OPENROUTER_API_KEY=your_openrouter_api_key
# Optional override. If omitted, the script auto-tries multiple free models.
OPENROUTER_MODEL=
# Max number of models to try per run (default: 3)
MAX_MODEL_ATTEMPTS=3
# Per-model timeout in milliseconds (default: 15000)
MODEL_TIMEOUT_MS=15000
